@*@using CarShop.Models.my
@model IEnumerable<Bicycle>*@

@{
    ViewBag.Title = "Корзина";
}

<h2 class="text-center">Ваша корзина</h2>

<div class="container mt-4">
    @*@if (!Model.Any())
    {
        <div class="alert alert-info text-center">
            <strong>Ваша корзина пуста.</strong> Добавьте товары, чтобы продолжить покупки!
        </div>
    }
    else
    {
        <table class="table table-hover">
            <thead class="thead-light">
                <tr>
                    <th>Товар</th>
                    <th>Количество</th>
                    <th>Цена</th>
                    <th>Итого</th>
                    <th>Действия</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            <img src="@item.img" alt="@item.name" style="width: 70px; height: auto;" class="me-2" />
                            @item.name
                        </td>
                        <td>
                            <input type="number" class="form-control form-control-sm" value="1" min="1" style="width: 60px;" />
                        </td>
                        <td>@item.price ₽</td>
                        <td>@(item.price * 1) ₽</td>
                        <td>
                            <button class="btn btn-sm btn-danger">Удалить</button>
                        </td>
                    </tr>
                 }*@
            </tbody>
        </table>
        <div class="d-flex justify-content-between align-items-center mt-4">
            <h4>Общая стоимость: <span class="text-primary"> ₽</span></h4>
            <a href="/Checkout" class="btn btn-success btn-lg">Оформить заказ</a>
        </div>
    } 
    
